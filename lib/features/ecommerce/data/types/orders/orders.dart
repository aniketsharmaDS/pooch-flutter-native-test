enum OrderType { product, accessory, vetSubscription, poochSubscription }

enum OrderStatus { delivered, inTransit, cancelled, pending, unknown }

enum OrderRequestType { returnOrder, cancelOrder }

enum OrderActionBottomSheetMode { confirm, status }
