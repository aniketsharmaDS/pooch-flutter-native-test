name: poochcare
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1


environment:
  sdk: ">=3.11.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  dio: ^5.7.0
  get_it: ^9.2.1
  provider: ^6.1.2
  lottie: ^3.3.1
  auto_route: ^11.1.0
  flutter_dotenv: ^6.0.0
  dart_mappable: ^4.6.1
  flutter_screenutil: ^5.9.3
  flutter_localizations:
    sdk: flutter
  flutter_svg: ^2.2.4
  google_sign_in: ^7.2.0
  hydrated_bloc: ^11.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5
  pinput: ^6.0.2
  dropdown_button2: ^3.0.0
  skeletonizer: ^2.1.3
  cached_network_image: ^3.4.1
  permission_handler: ^12.0.1
  image_picker: ^1.2.1
  file_picker: ^10.3.10
  device_info_plus: ^12.4.0
  image: ^4.8.0
  crop_your_image: ^2.0.0
  mime: ^2.0.0
  path: ^1.9.1
  open_filex: ^4.7.0
  intl: ^0.20.2
  fluttertoast: ^9.0.0
  device_preview: ^1.3.1
  firebase_core: ^4.6.0
  firebase_auth: ^6.4.0
  firebase_messaging: ^16.0.2
  flutter_local_notifications: ^19.4.0
  flutter_secure_storage: ^9.2.4
  video_player: ^2.11.1
  glassmorphism_ui: ^0.3.0
  flutter_facebook_auth: ^7.1.5
  firebase_crashlytics: ^5.2.0
  fl_chart: ^1.2.0
  share_plus: ^10.0.0
  url_launcher: ^6.3.0
  collection: ^1.19.1
  syncfusion_flutter_calendar: ^32.1.23
  calendar_view: ^2.0.0
  http: ^1.6.0
  webview_flutter: ^4.13.1
  flutter_pdfview: ^1.4.4
  fpdart: ^1.2.0
  easy_localization: ^3.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.13.0
  auto_route_generator: ^10.5.0
  dart_mappable_builder: ^4.6.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true
  
  assets:
    - .env
    - assets/translations/en/
    - assets/translations/ar/
    - assets/icons/png/generic/
    - assets/icons/png/nudges/
    - assets/icons/png/transitions/
    - assets/icons/svg/actions/
    - assets/icons/svg/app_bar_icons/
    - assets/icons/svg/comment_icons/
    - assets/icons/svg/community/
    - assets/icons/svg/coupons/
    - assets/icons/svg/orders/
    - assets/icons/svg/documents_icon/
    - assets/icons/svg/drawer/
    - assets/icons/svg/generic/
    - assets/icons/svg/nudges/
    - assets/icons/svg/social/
    - assets/icons/svg/specialist/    
    - assets/icons/svg/tabs/
    - assets/icons/svg/transitions/
    - assets/images/png/symptoms/
    - assets/images/png/explore/
    - assets/images/png/register/
    - assets/images/png/transitions/
    - assets/images/png/bcs/
    - assets/lottie/
    - assets/images/png/verify_biometrics/
    - assets/videos/
    - assets/images/jpg/onboarding/
    - assets/images/png/profile/
    - assets/images/png/orders/
    - assets/images/png/leaderboard/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Gilroy400
      fonts:
        - asset: assets/fonts/Gilroy-Regular.ttf
    - family: Gilroy500
      fonts:
        - asset: assets/fonts/Gilroy-Medium.ttf
    - family: Gilroy600
      fonts:
        - asset: assets/fonts/Gilroy-SemiBold.ttf
    - family: Gilroy700
      fonts:
        - asset: assets/fonts/Gilroy-Bold.ttf
    - family: Gilroy900
      fonts:
        - asset: assets/fonts/Gilroy-ExtraBold.ttf

  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
